{
  "name": "schibsted-and-vend-blocker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Schibsted & Vend blocker extension for Firefox and Chromium based browser",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "start:firefox": "web-ext run --source-dir dist/firefox",
    "start:chromium": "web-ext run --target chromium --source-dir dist/chromium",
    "package:firefox": "web-ext build --source-dir dist/firefox --artifacts-dir web-ext-artifacts/firefox --overwrite-dest",
    "package:chromium": "web-ext build --source-dir dist/chromium --artifacts-dir web-ext-artifacts/chromium --overwrite-dest",
    "package": "npm run package:firefox && npm run package:chromium",
    "lint": "web-ext lint --source-dir dist/firefox"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "web-ext": "^8.3.0"
  }
}
